<?xml version="1.0" encoding="UTF-8"?>
<suppressions
	xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">
    <suppress>
        <notes><![CDATA[
        spring-security-*: CVE-2018-1258 is a false positive. The vulnerability is in
        Spring Framework 5.0.5 (a method-security authorization bypass when used with
        Spring Security), fixed in Spring Framework 5.0.6. The scanner matches it against
        the spring_security CPE because the NVD entry lists Spring Security as affected.
        IZ Gateway uses Spring Framework 6.2.x, which is not affected.

        This rule replaces five earlier sha1-pinned rules for the same CVE. Those hashes
        were for older spring-security jars, so they stopped matching after the Spring
        upgrade (IGDD-2353) and the finding resurfaced on spring-security-core and
        spring-security-oauth2-resource-server 6.5.11 in CI run 35600127560.

        Match on filePath, not packageUrl. The scan target is the Spring Boot fat jar,
        and dependency-check records no packages and no identifiers for the jars nested
        under BOOT-INF/lib, so a packageUrl rule matches nothing. Verified in CI run
        35603999288, where the packageUrl form reported "Suppression Rule had zero
        matches". A filePath regex survives dependency upgrades; a sha1 does not.
        Rewritten: 2026-09-21 (IGDD-2396)
        ]]></notes>
        <filePath regex="true">.*/BOOT-INF/lib/spring-security-.*\.jar</filePath>
        <cve>CVE-2018-1258</cve>
    </suppress>
    <suppress>
        <notes><![CDATA[
        tomcat-embed-core: CVE-2026-66299 is a false positive for embedded Tomcat usage.
        The vulnerability is an unbounded buffer for undelivered messages in the WebSocket
        chat example web application bundled with the full Apache Tomcat distribution,
        allowing a slow client to exhaust memory. Apache's own advisory states users who
        removed the examples web application are not affected. Our use of tomcat-embed does
        not include the examples web application - this is Spring Boot's embedded Tomcat
        (tomcat-embed-core/-el/-websocket only, pulled in via izgw-bom's tomcat.version
        property), which never bundles the examples webapp in the first place. Confirmed as
        a known false-positive pattern on the Apache Tomcat security mailing list thread for
        this CVE.

        Fixed versions (11.0.25 / 10.1.58 / 9.0.121) had not been released as of this
        writing - confirmed tomcat-embed-core 10.1.58 does not yet exist on Maven Central
        (still under development). Revisit once 10.1.58 ships; this suppression may become
        unnecessary (harmless if left in place - it will simply stop matching).
        Added: 2026-08-08 (IGDD-3254)
        ]]></notes>
        <filePath regex="true">.*/BOOT-INF/lib/tomcat-embed-core-.*\.jar</filePath>
        <cve>CVE-2026-66299</cve>
    </suppress>
</suppressions>
